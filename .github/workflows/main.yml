name: Network Info

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]

    steps:
      - uses: actions/checkout@v2
      
      - uses: actions/setup-python@v2
        with:
          python-version: '3.8'
      - run:
          pip install -r requirements.txt
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run main.py
        run: |
          python main.py
        env:
          aws_access_key_id: ${{ actions-env.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ actions-env.AWS_SECRET_ACCESS_KEY }}
          aws_default_region: ${{ actions-env.AWS_DEFAULT_REGION }}
          aws_bucket: ${{ actions-env.AWS_BUCKET }}
          aws_cf_id: ${{ actions-env.AWS_CF_ID }}

      - name: Add & Commit
        uses: EndBug/add-and-commit@v4.4.0
        with:
          message: 'Update the Network Configuration'
          ref: 'gh-pages'
        env:
          GITHUB_TOKEN: ${{ actions-env.REPO_TOKEN }}

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ actions-env.REPO_TOKEN }}
          branch: ${{ github.ref }}
